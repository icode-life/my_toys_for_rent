<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @toy.name %>
</p>

<p>
  <strong>Detail:</strong>
  <%= @toy.description %>
</p>
raise
<p>
  <strong>Price:</strong>
  <%= @toy.price %>
</p>

<p>
  <strong>Category:</strong>
  <%= @toy.category %>
</p>

<p>
  <strong>Photo:</strong>
  <%= cl_image_tag @toy.photo, width: 400, height: 300, crop: :fill %>
</p>

<td><%= link_to "Back to list", toys_path %></td>

<td>
  <% if policy(@toy).edit? %>
    <%= link_to "Edit", edit_toy_path(@toy) %>
  <% end %>
</td>

<td>
<% if policy(@toy).destroy? %>
  <%= link_to "Remove", toy_path(@toy), method: :delete, data:{confirm: "Are you sure?" } %>
<% end  %>
</td>

<p>
  <%= link_to "Book this toy", new_toy_booking_path(@toy), class: "btn btn-success" %>
</p>
