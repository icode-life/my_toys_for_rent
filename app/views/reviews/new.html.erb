<p> Your review of <%= @toy.name %> : </p>

<%= simple_form_for [@toy, @review] do |f| %>
<%= f.input :content %>
<%= f.input :rating, collection: 0..5 %>
<%= f.submit "Add my review", class: "buttonMedium" %>
<% end %>

<%= link_to "Back to all the toys", toys_path %>
